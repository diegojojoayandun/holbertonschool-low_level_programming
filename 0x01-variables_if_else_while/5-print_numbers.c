#include <stdio.h>

/**
 * main - prints the alphabet using the putchar function
 * Return: 0 if success witout any error
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}

	printf("\n");
	return (0);
}
