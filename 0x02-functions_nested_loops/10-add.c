#include "main.h"

/**
 * add - function to add two numbers take from parameters
 * @a: integer that takes the first param
 * @b: integer that takes the second param
 * Return: retrive a+b result
 */
int add(int a, int b)
{
	int c;

	c = a + b;
	return (c);
}
