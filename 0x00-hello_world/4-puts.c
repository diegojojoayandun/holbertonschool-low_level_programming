#include <stdio.h>

/**
 * main - entry point, print phrase usin the put command
 *
 * Return: 0 if was success or any other number if not
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
