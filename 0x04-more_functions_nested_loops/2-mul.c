#include "main.h"

/**
 * mul - funtion that multiplica two numbers taken as paramters
 * @a: integer, first parameter
 * @b: integer, second parameter
 * Return: the multiplication of a*b
 */
int mul(int a, int b)
{
	return (a * b);
}
